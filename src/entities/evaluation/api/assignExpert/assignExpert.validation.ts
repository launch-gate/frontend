import {
  assignmentRequestSchema,
  assignmentSchema,
} from "../../model/evaluation.validation";

export const assignExpertRequestSchema = assignmentRequestSchema;
export const assignExpertResponseSchema = assignmentSchema;
