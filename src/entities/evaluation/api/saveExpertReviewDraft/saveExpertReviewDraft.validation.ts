import {
  reviewDraftRequestSchema,
  reviewSchema,
} from "../../model/evaluation.validation";

export const saveExpertReviewDraftRequestSchema = reviewDraftRequestSchema;
export const saveExpertReviewDraftResponseSchema = reviewSchema;
