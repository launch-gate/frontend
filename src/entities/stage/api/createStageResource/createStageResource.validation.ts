import {
  resourceRequestSchema,
  resourceSchema,
} from "../../model/stage.validation";

export const createStageResourceRequestSchema = resourceRequestSchema;
export const createStageResourceResponseSchema = resourceSchema;
